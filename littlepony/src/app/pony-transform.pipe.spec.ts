import { PonyTransformPipe } from './pony-transform.pipe';

describe('PonyTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new PonyTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
